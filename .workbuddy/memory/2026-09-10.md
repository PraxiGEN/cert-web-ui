# 2026-09-10

- 将 `cert-web-ui` 从 Python/Flask 重写为 **Go 单二进制**（先讨论方案，用户确认「一次性全做」）。
- 架构分包：`config` / `store` / `ca` / `api` / `scheduler`(main)。零外部依赖，`net/http` 路由（Go 1.22 模式）。
- 前端 `web/index.html` 运行时读取（最初用 `//go:embed` 在该 Docker 构建链报 "embed imported and not used"，改读文件更稳妥，Dockerfile 把 web 目录拷进最终镜像）。
- 功能：签发(包装 step CLI)、列表/下载、续签、删除、详情(x509 解析过期/SAN/序列号)、吊销、后台自动续签调度(`RENEW_INTERVAL`/`RENEW_BEFORE`)、临期标红预警、`UI_PASSWORD` 门禁。
- Dockerfile 多阶段 `golang:1.22-alpine` → `alpine:3.20`(含 step CLI)。已 `docker build` 成功并冒烟测试通过（config/health/首页/certs/401 鉴权/issue 错误返回）。
- 运行：`docker build -t cert-web-ui .` 后 `docker run -p 8080:8080 -e STEP_CA_URL=... -e STEP_ROOT=... -e STEP_PASSWORD_FILE=... -e CERT_OUTPUT_BASE=... cert-web-ui`
- 注意：原 `app.py` 已废弃并删除；吊销依赖 step-ca 开启 revoke 接口与 provisioner 权限，可能失败，前端已做失败提示。

## UI 重构（第二轮）
- 按用户要求重做前端为**左导航 + 右内容**单页，三个 tab：**首页(Dashboard) / 证书管理 / 后端管理**；不做个人资料/访问设置（密码仍走 `UI_PASSWORD` 环境变量），中间证书只下根证书。
- 签发表单改动：移除「输出文件夹」框，新增「名称」框（仅英文，作文件夹名，`IssueRequest.Folder`→`Name`）；有效期默认 1 年；密钥类型补全 `EC P-256/P-384/P-521` + `RSA 2048/4096`（`ca/issue.go` switch 扩展）。
- 后端新增 `ca/status.go:Health`（调 `step ca health` 真实探测 CA 连通性；注意 `step ca health` 不接受 `--password-file`，只接 `--ca-url`+`--root`）；`api/router.go` 加 `GET /api/backend/status` 与 `GET /api/backend/root`（根证书下载，专线路由限 `STEP_ROOT`）。
- 已 `docker build` 重构建成功并冒烟：后端 status 真实探测(无 CA 返回 reachable:false+真实错误)、root 路由存在(404 因文件不存在)、首页含三导航、密码 401 生效。

## UI 对齐 Docker Copilot 风格（第三轮）
- 参考 `DongShull/Docker-Copilot-React`：侧栏改为**白底(浅)/slate-800(深)** 固定栏(256px)，顶部 Logo+标题`Cert Web UI`+副标题"证书管理平台"，分割线，导航项带 SVG 图标(激活项左侧主色高亮条+主色浅底)，底部三图标**分段主题控件**(☀浅色/🌙深色/🖥系统) + 版本/作者信息卡。
- 主题默认改为 `system`(跟随系统)，持久化 key `cert-ui-theme`；主色 `--primary:#3b82f6`。新增 `--sidebar-bg/--sidebar-text/--sidebar-active-bg/--seg-bg` 等变量，浅色与深色分别覆盖。
- CA 状态点改为 `.ca-dot` 小圆点 + 文案(`sideCaText`)，移除旧 `.sidebar-foot`/`.dot-theme`。
- ⚠️ 同一文件并发 Edit 会触发写覆盖(lost-update)：仅最后一个编辑生效。务必**逐个顺序**编辑同一文件。已 `docker build` 通过并冒烟验证(3 个 theme-opt、品牌标题、三导航、旧类名清零)。

## 导入外部证书（第四轮）
- 新增「导入外部证书（归档）」功能：仅归档，与 step-ca 签发证书严格区分。决策：用户确认按"仅导入归档"实现，上传证书标记 `origin=imported`，不支持续签/吊销（避免稀释续签链路）。
- `store.Metadata`/`store.CertEntry` 加 `Origin` 字段(issued/imported)；`store.parseCertData` 由未导出改为导出 `ParseCertData`（`ca` 包需直接校验 PEM，否则跨包编译报错 undefined）。
- 后端：`POST /api/certs/import`（`api/router.go`+`api/handlers.go`→`ca.Import`，新文件 `ca/import.go`）：校验 name(`SanitizeFolder` 防穿越)、crt 必填且合法 PEM、可选 key 合法 PEM；写 `OUTPUT_BASE/<name>/<name>.crt`(+`.key`)+`metadata.json`(`Origin=imported`, `AutoRenew=false`)。防覆盖：同名文件夹已存在则中止。
- 前端：证书管理页加独立「导入外部证书」卡片(名称/crt/key 文本域)；`certRow` 按 origin 显示「导入」标签，imported 隐藏续签/吊销按钮(仅证书/私钥下载、详情、删除)。导入证书计入临期/过期统计但排除自动续签。
- `docker build` 通过并冒烟：导入成功(`origin=imported`)、重复同名拦截、非法/空 PEM 拦截均符合预期。

## 签发表单精简（第五轮）
- 移除「主域名 *」必填框，仅保留「域名 / SAN（每行一个，第一行作为证书主域名 CN）」多行框。用户决策：保留 SAN 单框即可（方案1）。
- 后端 `ca/issue.go`：`domain`(=证书 CN) 改为可选；为空时从 SAN 第一行取 CN；两者皆空才报「请至少填写一个域名 / SAN」。step 自动把 subject 加进 SAN，故只填一个域名也有效。`parseSANs` 上移复用。
- 前端 `web/index.html`：删除 `name="domain"` 输入框；提交 `doIssue` 不再送 `domain` 字段。
- `docker build` 通过 + 冒烟：空校验返回新错误、SAN-only 时派生 CN 并尝试 `step ca certificate`（因无 CA 根文件停在连接层，符合预期）。

## 部署与 compose（第六轮）
- 用户确认 `cert-web-ui` 仅提供 HTTP（Go `http.Server`，监听 :8080），UI 自身不做 TLS；要 HTTPS 需在前面加 nginx/caddy 反代做 TLS 终止。step-ca 后端 URL 仍走 https（UI→step-ca 用 `STEP_CA_URL`）。
- **重要：`main.go` 为静默启动设计**——启动成功不打印任何日志；只有「读不到 index.html（警告）」或「端口占用（panic）」才打印。因此 `docker logs` 显示 "No log line matching the '' filter"（空日志）属正常，≠ 故障。用户曾误以为没日志=坏了，实为 app 正常在跑（HTTP 能访问即证）。
- 落盘修正版 `docker-compose.yml`（项目根）：修复用户原 compose 两处致命错——① `STEP_PASSWORD_FILE` 原写成密码明文，应为文件路径 `/home/step/secrets/password`；② 输出卷原覆盖 `/home/step/certs` 致根证书 `STEP_ROOT` 读不到，改为独立 `/certs-output` 挂载 + `CERT_OUTPUT_BASE=/certs-output`。
- 提醒边界：「UI 首页能打开」≠「能签发」——首页不依赖 step-ca 根证书，后端/签发异常时首页仍正常；须点「后端管理」看 CA 状态或 curl `/api/backend/status` 验证。

## 日志增强（第七轮）
- 用户要求容器像常规服务一样打印详细日志便于排查（原 main 静默启动无日志，曾误判为故障）。
- 新增 `api/logging.go`：`LoggingMiddleware` 包裹整个路由（`*http.ServeMux`），输出 `[ACCESS] 远程地址 方法 路径 状态码 字节 耗时`；`log.SetFlags(log.LstdFlags|log.Lmicroseconds)` 全局带时间戳（含微秒）。
- `main.go`：启动打印 `[INFO]` 横幅（版本/端口/CA 后端/输出目录/根证书/续签间隔与提前量/密码门禁开关）；原 `panic(err)` 改为 `log.Fatalf("[FATAL] ...")`。
- `api/handlers.go`：各 handler 成功打 `[INFO]`、失败打 `[ERROR]`（含具体失败原因，便于排查签发/导入/续签/吊销/删除失败）；`withAuth` 密码错误打 `[WARN]`；后端不可达/根证书未配置打 `[WARN]`。
- `scheduler.go`：续签订时器启动 + 每次扫描打 `[INFO]`（证书总数、自动续签候选数）。
- `docker build` 通过 + 冒烟：启动横幅与 `[ACCESS]` 访问日志均正常输出，确认日志管道工作。

## 证书管理分页（第八轮）
- 用户要求「证书管理」标题下方加横向分段状态栏，把三块内容分页显示。
- 命名定为：**签发证书** / **导入证书** / **证书列表**（原「管理已签发证书」精简为名词短语，并带数量徽标）。
- 实现：`view-certs` 内标题下加 `.subtabs`（3 个 `.subtab` button，data-sub=issue/import/manage）；三个卡片直接加 `class="card subpanel" data-sub=...`，非默认项加 `hidden`；默认显示 issue。
- CSS：`.subtabs` 分段控件（复用 `--seg-*` 变量，与侧栏主题控件风格一致）；`.subtab` 覆盖 button 默认橙色底与 `margin-top`。
- JS：新增 `showSub(sub)` 切换 active + hidden；`renderStats` 同步 `#manageCount` 显示证书数量。
- 纯前端改动；`docker build` 通过 + 冒烟：服务 HTML 含 subtabs、三个 data-sub（各 2 次=按钮+面板）、showSub，符合预期。
- 🐞 修复「导入证书/证书列表」分页空白：初始隐藏误用 **HTML `hidden` 属性**，而 `showSub` 用 `classList.toggle('hidden')` 操作的是 **CSS 类**，移除类不会移除属性 → 面板永隐藏。统一改为初始即用 `class="... hidden"` 类。教训：项目内隐藏面板一律用 `.hidden` 类，禁用原生 `hidden` 属性，避免与 classList 切换不一致。

## 后端管理页优化（第九轮）
- 用户反馈：CA 地址在「连接状态」与「根证书」两卡重复 → 删除根证书卡里的 CA 地址行（含 JS 中 `bkCaUrl` 赋值，避免取空元素报错）。
- 「连接状态 / 根证书」两卡的按钮由底部整行改为**标题右侧横向布局**：新增 `.card-head`（flex + space-between），把 h3 与 button 并排。
- 用户另询问「后端管理还能有哪些功能」→ 给出建议清单（根证书详情解析、provisioner 列表、CA 延迟、自动续签状态+手动触发、中间证书下载、只读配置展示等），待用户选定后实施。
- 纯前端改动；`docker build` 通过（新增 `.card-head` 样式、`bkCaUrl` 残留=0）。

## 根证书详情 + 自动续签状态（第十轮）
- 按钮垂直居中：`.card-head button { margin: 0; align-self: center; }`（原仅覆盖 margin-top，h3 与按钮同行垂直居中）。
- **架构调整**：把调度逻辑从根级 `scheduler.go`（package main）抽到独立包 `scheduler/`（`package scheduler`），暴露 `State`/`Start(cfg)`/`Status()`/`ScanNow(cfg)`。原因：`api` 包需读调度状态并手动触发，但 main 不能被 api 反向 import（import 环）。删除根 `scheduler.go`，main 改调 `scheduler.Start(cfg)`，**Dockerfile 的 `COPY scheduler.go ./scheduler.go` 同步改为 `COPY scheduler ./scheduler`**。
- 新增 `ca/rootinfo.go`：`RootInfoOf(cfg)` 用 crypto/x509 解析 STEP_ROOT，输出路径/Subject/Issuer/序列号/有效期/剩余天数/是否过期/SHA-256 指纹/签名算法/公钥算法(EC 曲线·RSA 位数)/是否CA/是否自签；错误写入结构体不返回 error。
- 新增接口：`GET /api/backend/rootinfo`、`GET /api/renew/status`、`POST /api/renew/scan`（均 withAuth）。
- 前端：根证书卡改为详情容器 `#rootInfo`（替代原字面量 `STEP_ROOT` 瑕疵）；新增「自动续签」卡 `#renewInfo` + 「立即扫描」按钮；`loadBackendStatus` 用 Promise.all 拉三接口并渲染；新增 `fmtTime`/`kvRow`/`renderRootInfo`/`renderRenew`/`doScan`。
- `docker build` 通过 + 冒烟：rootinfo 正确解析自签根证书（RSA 2048/SHA256-RSA/自签/is_ca）；renew/status 返回间隔与 next_scan；手动 scan 后 last_scan 实时更新，日志输出「手动触发自动续签扫描」。

## 后端路线决策（第十一轮，讨论，未实现）
- 用户提问：现需单独部署 step-ca 才能用 UI，能否「镜像内置 step-ca」或换可内置后端以求部署简化；要求先讨论不写码。
- 给三路线对比：①镜像内置 step-ca 双进程（需 supervisor + init 编排，重且复杂）；②Go 内置自签 CA（`crypto/x509` 自己当 CA，零依赖、单容器、进程内签发/续签/吊销）；③保留 step-ca + compose 一键（能力全）。并列出 cfssl / 内嵌 step-ca 库 / Vault PKI 等候选，结论：不如直接用标准库自建。
- 关键判据：是否需要 ACME / OIDC / SSH CA / step 生态；仅「内网签发+续签+到期管理」则自建 CA 足够。
- **决策：路线 ②「自建 Go 内置 CA」**（用户确认）。
- 含义：可删除 step CLI、`STEP_CA_URL`、`STEP_ROOT`、`STEP_PASSWORD_FILE`、`STEP_BIN`；签发/续签/吊销/下载在进程内实现；失去 ACME/OCSP/多 provisioner/step 生态。旧 step-ca 已签证书无法续签（按「只写向前代码」不迁移）。
- 待：细化落地设计（CA 数据目录、根 CA 生成策略、吊销/CRL）、前端后端管理页改写，然后开工。

## 自建 Go 内置 CA 落地（第十二轮）
- 用户拍板：根 CA 有效期 **10 年**、叶子证书默认 **1 年**（即与现状一致），算法 EC P-256，不再依赖 step-ca。
- `config/config.go` 重写：删除 `CACertURL`/`PasswordFile`/`StepBin`（及 STEP_CA_URL/STEP_ROOT/STEP_PASSWORD_FILE/STEP_BIN）；新增 `CA_HOME`(默认 /data)、`CA_NAME`(根 CN)、`CA_ROOT_CERT`、`CA_ROOT_VALIDITY`(87600h)、`CA_LEAF_VALIDITY`(8760h)；`Root` 默认 `CAHome/root_ca.crt`，`OutputBase` 默认 `CAHome/certs`。
- 新增 `ca/pki.go`：`InitRoot(cfg)` 幂等+并发安全——有根则**加载**（绝不静默覆盖，避免误轮换根），无则生成 EC P-256 自签根（KeyUsage CertSign|CRLSign|DigitalSignature、CA:TRUE pathlen:1）；`generateLeafKey`(EC P-256/384/521、RSA 2048/4096)、`parsePrivateKey`(PKCS1/EC/PKCS8)、`buildLeafTemplate`(自动分 DNS/IP，RSA 才加 KeyEncipherment)、`randSerial`(128bit)、`writePEMFile`。
- `ca/issue.go` 重写：进程内 `x509.CreateCertificate`；CN 不在 SAN 时自动前置补入；文件名仍以 CN 命名。
- `ca/renew.go` 重写：续签 = 用原私钥**重签**（保留 CN/SAN/原时长），新序列号并清除吊销标记；`origin=imported` 直接拒绝。
- `ca/revoke.go` 重写：本地吊销记录 `revoked.json`（按序列号去重、幂等）+ 生成 `crl.pem`（`x509.CreateRevocationList`，24h）；新增 `RevokedCount`/`CRLPath`。
- `ca/status.go` 重写：`Health` 改为本地 CA 就绪探测（根可加载 + IsCA + 未过期），无网络调用。
- `store`：`Metadata`/`CertEntry` 增加 `Revoked`/`RevokedAt`，`ListCerts` 填充。
- `api`：`/api/config` 改返回 `ca_home`；`/api/backend/status` 改返回 `{ready, ca_home, root_path, message, revoked}`；新增 `GET /api/backend/crl`。
- `main.go`：启动日志改自建 CA 口径，启动时调 `ca.InitRoot(cfg)`。
- `Dockerfile`：删除下载 step CLI 的整段 → 仅 alpine + ca-certificates，镜像更小、构建不再依赖 GitHub。
- `docker-compose.yml`：改为**单容器**，卷 `/mnt/sdb/1000/Docker/cert-web-ui/data:/data`；env 仅 CA_HOME/CERT_OUTPUT_BASE/CA_NAME/CA_ROOT_VALIDITY/CA_LEAF_VALIDITY/RENEW_INTERVAL/RENEW_BEFORE。
- 前端：文案 step-ca→内置 CA；`renderBackend` 改用 `ready`/`ca_home`；移除 `CA_URL`；根证书卡新增「下载 CRL」按钮（`.head-actions`）；列表对 `revoked` 显示「已吊销」徽标。
- 验证（docker build + 端到端冒烟，全程无 step-ca）：根 CA 自动生成（有效期至 2036-09-07）；`openssl verify` 对 EC/RSA 叶子均 `OK`；仅填 SAN 可签发；空校验报「请至少填写一个域名 / SAN」；续签 serial 变化、SAN 保留、链仍 OK；吊销幂等、`crl.pem` 含吊销序列号（ecdsa-with-SHA256）；重启后根指纹不变（日志「根 CA 已加载」）；带私钥导入的证书续签被 `origin=imported` 拒绝；`/api/backend/crl`→200、`/api/backend/root`→200。
- ⚠️ **再次踩坑**：同一条消息内对**同一文件**发多个 Edit 会写覆盖（本次 store.go、main.go、index.html 各丢失若干处，导致首次 docker build 编译失败）。教训：同一文件必须**一条消息一个 Edit**，改完立刻 grep 复核。
- 本机试用运行方式（已验证）：`docker build -t cert-web-ui:local .` + `docker run -d --name cert-web-ui -p 16850:8080 -v cert-ca-data:/data -e CA_NAME="Home.lan Root CA" --restart unless-stopped cert-web-ui:local`，访问 `http://127.0.0.1:16850`。CA 数据存命名卷 `cert-ca-data`（首启自动生成 EC P-256 根 CA，10 年）。冒烟已签 2 张示例（ha-home/EC P-256/自动续签、nas/RSA 2048）。
- 证书列表 UI 微调（纯前端）：① 剔除分页栏「证书列表 (N)」数量徽标（删 HTML `#manageCount`、JS 赋值、`.subtab-count` 样式）；② 把「自动续签/导入/已吊销」徽标从**域名列**移出，改为独立**「状态」列**（`<td><div class="state">`），域名列只剩域名+文件夹路径——避免徽标夹在域名与 SAN 之间撑宽列、小屏更乱；③ 操作列 `.actions` 由 `display:block + margin-right` 改 `display:flex; flex-wrap:wrap; gap:6px`，窄屏换行后**上下间距不再贴住**。连带：表头 4→5 列、`toggleDetail` 的 `colspan` 4→5、`.badge.auto` 去掉 `margin-left:6px`（改由 `.state` 的 gap 统一控制）。
- 证书列表第二轮排版（纯前端）：① SAN 列**每个域名一行**（`c.sans.map(s=>'<div>'+s+'</div>')`），固定换行不随屏宽变化；② 表格改 `#certList table { table-layout: fixed; min-width: 860px }` + 容器 `overflow-x:auto` + 列宽 `th:nth-child` 百分比 **22% / 16% / 13% / 19% / 30%**（域名/SAN/状态/过期/操作），列宽不再被内容撑变形；③ 过期列拆两行：徽标 `exp.short`（剩余 N 天）+ 日期 `exp.date` 小字（`fmtExpiry` 新增 short/date 字段，首页「最近签发」仍用 `text` 保持 4 列不变）；④ **新增复制能力**：`copyToClipboard()`（优先 Clipboard API，`window.isSecureContext` 为假时**降级 `execCommand('copy')`**——部署机走 http 非安全上下文必须靠降级）+ `flashBtn()` 按钮反馈「已复制/失败」。域名旁加「复制」小按钮；操作列 `证书`→`下载证书`、`私钥`→`下载私钥`；详情面板顶部加「复制证书 PEM / 复制私钥 PEM」（前端 `fetch` 下载路由取文本再写剪贴板，**零后端改动**）。
- PKI 架构讨论（第十三轮，仅讨论未改码）：用户问「是否有必要像 step-ca 一样加中间证书」。**结论：不必**。中间层唯一买到的价值是把「信任锚（根）」与「在线签名密钥」分离——前提是根私钥**离线**；本项目根私钥就在 `/data`，加中间层后两把密钥同卷、威胁模型不变，纯增复杂度（叶子 `.crt` 须写成 leaf+intermediate 全链、客户端须信任**根**而非中间、CRL 签名者由根变中间、续签/下载/导入/前端展示全要跟改，约 6 文件）。
- **关键事实：门已开着**——根模板 `MaxPathLen: 1`（`ca/pki.go:112`），将来真要加中间层，用现有根签一个即可，**不需重建根、不需客户端重新信任**（旧叶子根直签仍有效，新叶子走中间链也验得过）。故当前不加零技术债。
- 三选项：A 单层根（现状，保持）｜B 根+中间但两把密钥都留 `/data`（得「轮换中间免客户端重信任」，安全收益为 0，属运维灵活性）｜C 根离线+中间在线（真 step-ca 模型，与「单容器/首启自动生成」设计冲突，不推荐）。
- 顺带发现 step-ca 命名残留：`ca/rootinfo.go:43` 报错文案「未配置根证书路径（STEP_ROOT）」、`api/handlers.go:190` 注释「STEP_ROOT 根证书下载」——自建 CA 后应清理，以免误导（讨论中间证书时尤易让人误以为仍接外部 step-ca）。
- 证书列表「状态」列升级（纯前端）：由原「自动续签/导入/已吊销」三个横排徽标，改为**证书状态标签竖向堆叠、每条一行、颜色区分**。主状态**四选一**（优先级 `revoked` > `expired` > `warning` > 正常）→ `已吊销`(紫) / `已过期`(红) / `临期`(橙) / `正常`(绿)；附加标签 `自动续签`(蓝)、`导入`(灰)。`certRow` 用 `stPrimary + auto + originBadge` 拼接（原 `revBadge` 删除，原 fallback `—` 不再需要）。CSS：`.state` 由 `flex-wrap:wrap; gap:6px` 改 **`flex-direction: column; align-items: flex-start; gap:4px`**（`flex-start` 必须，否则 pill 被拉伸满列宽）；新增 `.badge.rev`(紫) 与 `.badge.imp`(灰，色取 `--folder`，原「导入」用的是无修饰 `.badge`)。
- 新增主题变量 `--rev`：浅色 `#8e24aa`、深色 `#ce93d8`，**三处主题块都要加**（`:root` 浅色、`@media (prefers-color-scheme: dark) :root:not([data-theme="light"])`、`:root[data-theme="dark"]`）。已 `docker build` 通过 + HTML 校验（`--rev`/`badge rev`/`badge imp`/`flex-direction: column; align-items: flex-start` 各 1，`stPrimary` 6 处），容器已重启、数据卷保留。
- ⚠️ 遗留：状态列的「临期/已过期」与「过期」列的徽标文案**语义重复**（过期列仍显示 `临期 N 天`/`已过期`）。已在回复中向用户说明，待其决定是否把过期列改为纯日期+天数、去掉状态词。未擅自改动（用户只要求改「状态」一栏）。
- **方案 A 落地**（用户选定）：过期列去掉状态词。`fmtExpiry` 新增 `days` 字段（正常/临期均 `剩余 N 天`、已过期 `过期 N 天`，用 `Math.abs(days)` 防负数）；过期列改用 `exp.days`。`text`（含状态词+日期）**保留**给首页「最近签发」表（该表无状态列，仍需「临期/已过期」措辞）。职责划分：**状态列=语义，过期列=时间**。
- **step 残留全量清理**（用户要求「剔除原 step 相关所有代码、精简项目」）：全项目 `grep -rniI step` 命中仅 6 处，已全清——① `web/index.html:6` 浏览器标题 `Step-CA 证书管理` → `Cert Web UI · 证书管理`（**唯一用户可见的残留**）；② `api/handlers.go:190` 注释去 STEP_ROOT；③ `ca/rootinfo.go:43` 报错文案 `（STEP_ROOT）` → `（CA_ROOT_CERT）`；④ `ca/import.go:30` 注释去 step-ca；⑤ `ca/renew.go:18` 注释去「原 step 流程」；⑥ `config/config.go:10` 包注释去 step-ca / step CLI。另：Dockerfile 构建阶段 `COPY web ./web` 是**死拷贝**（Go 编译不读 web，前端由运行时 `os.ReadFile` 加载），已删。
- 复核：`grep -rniI step .`（排除 .git/.workbuddy）**零残留**；`docker build` 通过、容器重启、数据卷保留（nas / ha-home 仍在）、`/api/backend/status` 返回 `ready:true`。
- ⚠️ **待用户确认**：`web/index.html` 含 **75 个 `data-page-node-id="..."` 属性**（分布在 `<html>`/`<head>`/`<meta>` 及各元素上，疑为某 HTML 编辑/导入工具注入的产物，运行时无任何用途）。因担心是某个可视化编辑器的节点映射 ID、删除会破坏其工作流，**未擅自删除**，已在回复中询问是否一并清除。
- **布局重构（纯前端 `web/index.html`）**：左侧固定侧栏 → **上中下三段式**。① **上**：新增 `header.app-header`（sticky top，满宽铺满 + 内层 `.header-inner` max-width 1280 居中），左侧 logo + 名称 + 简介（新常量 `APP_DESC`，`initFooter` 写入 `#brandSub`），右侧 CA 状态点 + 版本 + 作者 + 主题切换；② **中**：`.content` 改为 `max-width:1280px; margin:0 auto` 居中；③ **下**：新增 `nav.dock` 底部**悬浮 Dock**（`position:fixed; left:50%; translateX(-50%)`，圆角胶囊 + 阴影），3 个 `.dock-item`（图标 + 文字竖排），取代原 `.nav-item`；`showTab` 选择器 `.nav-item` → `.dock-item`。删除侧栏 CSS（`.sidebar`/`.brand`/`.side-divider`/`.nav`/`.nav-item`/`.nav-icon`/`.side-foot`/`.side-meta`），保留并复用 `.theme-switch`/`.theme-opt`/`.side-ca`/`.brand-logo`。**居中/最小比例/自适应**：`body { min-width: 960px }` 兜底不塌陷，`max-width:1280px` 上限居中，`@media (max-width:1080px)` 收紧内边距（媒体查询必须写在 `.content` 基础规则**之后**，否则同优先级被覆盖）。`.content` 底部 `padding-bottom:128px` 防 Dock 遮挡。
- ⚙️ **经验**：本次 12+ 处同文件改动改用**单个 Python 脚本顺序替换**（每步 `assert count==expected`），一次性完成且避免「一条消息内多次 Edit 同一文件互相覆盖」的老坑；脚本失败在写盘前中断，文件不受污染。残留校验：`<aside>`/`.nav-item`/`class="sidebar"`/`side-meta`/`nav-icon` 全为 0；`--sidebar-*` 变量仍保留（现作顶栏/Dock 配色 token，非残留）。`docker build` 通过、容器重启、数据卷保留。
- **Dock 上移 + 列表列宽重分配 + 手机端适配**：① `.dock` 的 `bottom: 20px → 34px`，阴影加强为双层 `0 14px 34px rgba(15,23,42,.22), 0 2px 8px rgba(15,23,42,.10)`，悬浮感更明显；`.content` 底部留白 128→140px（≤1080 档 124→136px）。② 列宽 `22/16/13/19/30` → **`22/11/10/17/40`**，`#certList table` 的 `min-width: 860px → 960px`。依据：操作列 6 个按钮（下载证书/下载私钥/续签/吊销/详情/删除）实测需 ≈362px，旧 30% 在 1216px 内容宽下只有 365px（减去 td 左右 padding 10px 仅剩 345px）故必然换行；新 40% 在 1216px 下 ≈486px（可用 466px，余量 ≈104px），在 960px 下限下 384px（可用 364px，刚好放下）。`.actions { flex-wrap: wrap; gap: 6px }` **保留**作为兜底换行能力。③ 新增 **`@media (max-width: 760px)`** 手机档：`body { min-width: 0 }` 放开整页最小宽度（页面本身适配手机），改由 `#certList { overflow-x: auto }` 让**表格单独横向滚动**承压；同时顶栏改纵向堆叠（`.header-inner { flex-direction: column }`）、`.header-right` 两端对齐、`.stats` 2 列、Dock 收至 `bottom:14px`、`.dock-item` 缩小内边距。
- 关键权衡记录：`body { min-width: 960px }`（用户要的「限制最小比例」）与「手机适配」在窄屏下天然冲突，解法是**分档**——≥1080 保最小宽度、≤760 放开，中间档靠 `#certList` 内部横向滚动。已向用户说明，若不愿放开可回退。
- **列宽回退 22/16/13/19/30 + 三档自适应 + 手机卡片**（用户认为该比例"看着舒服"，撤掉 22/11/10/17/40）：
  - 实测（Edge 无头注入脚本上报）：6 个按钮需 **320px**（下载证书/私钥各 66，续签/吊销/详情/删除各 42，gap 4×5）。`.actions` 内边距 11→9px、gap 6→4px 后，**≥1300px 视口**（表格 1170px、操作列可用 331px）一行放下；≤1240px 自然换行（即用户要的"保留换行能力"）。
  - **「分割线断开」根因**：`<td class="actions">` 用 `display:flex` 会让 td 不再是 table-cell，该列行分隔线消失 → 改为 `<td><div class="actions">`。
  - **手机卡片**：新增 `certCard()` 与 `renderCertList()`，按 `matchMedia('(max-width:760px)')` 切换表格/卡片；`certRow`/`certCard` 共用 `certBadges/certSans/certActions/certDomainCell` 防逻辑漂移。卡片内已内置 `<div class="detail">`，故 `toggleDetail` 在卡片模式不走建 tr 分支（已加兜底）。
  - ⚠️ **CSS 层叠坑**：三个 `@media` 原本写在 `.stats`/`.dock` 基础规则**之前**，同优先级下被其后基础规则覆盖 → 手机端 `.stats` 仍 4 列、网格最小宽度撑破视口。**已把全部响应式块移到 `<style>` 末尾**（此坑已犯两次，务必记住：媒体查询必须置尾）。
  - **断崖修复**：`body{min-width:960px}` 在 761~960px 强制整页横向滚动，与 ≤760 卡片冲突 → 改为 **≤960px 即放开**，由列表卡片内部横向滚动承压；表格 `min-width` 860→800。
  - **Dock 挤压**：`fixed + left:50%` 使元素可用宽度只剩视口一半，窄屏 Dock 文字竖排换行 → `width: max-content; max-width: calc(100% - 24px)` + `.dock-item{white-space:nowrap}`。
  - 手机卡片操作区（≤760）改 3 列网格（3+3，`padding: 8px 6px` 提升触控）。顺带修正 `.folder`：原选择器 `td.folder` 从未命中实际的 `<div class="folder">`。
  - 验证：iframe 精确视口 320/360/390/430/600/760/761/820/886/900/1000/1200/1300/1440 全部 `docSW ≤ iw` 且 `bad: []`；表格与卡片两种模式「详情」展开均 `display=block`；`docker build` 通过、容器重启、数据卷保留。
- ⚙️ **可复用工具**：`%TEMP%\devshot.py` —— 本地视觉验证服务（服务 web/ + 反代 /api 到 16850 + 注入脚本自动切页/测量/点击测试 + `/frame?w=<px>` 用 iframe 精确约束视口）。关键：Windows 下 Edge `--dump-dom` 最小视口固定 492px，**必须用 iframe 才能测手机宽度**；`--screenshot` 视口 = window − 24；`--hide-scrollbars` 会让测量少 15px（测宽度时不要加）。
